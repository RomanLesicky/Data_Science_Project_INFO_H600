# Data_Science_Project_INFO_H600

We are not uploading the 32.1 GB million playlist dataset directly. 

Each person whishing to use this Github repository need to, download locally the dataset. 
Then create a `data/` folder in the root of the repository, where all the `mpd.slice.*.json` are to be put. 

However, we will be uploading the post-Step 1 dataset to be directly used. 